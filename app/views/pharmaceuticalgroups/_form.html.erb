<%= form_with(model: pharmaceuticalgroup, local: true) do |form| %>
  <% if pharmaceuticalgroup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pharmaceuticalgroup.errors.count, "error") %> prohibited this pharmaceuticalgroup from being saved:</h2>

      <ul>
      <% pharmaceuticalgroup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :pharmaceuticalgroup_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
