<%= form_with(model: pharmaceuticalform, local: true) do |form| %>
  <% if pharmaceuticalform.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pharmaceuticalform.errors.count, "error") %> prohibited this pharmaceuticalform from being saved:</h2>

      <ul>
      <% pharmaceuticalform.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :pharmaceuticalform_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
